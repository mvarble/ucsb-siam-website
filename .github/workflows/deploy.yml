name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    environment: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the site
        run: |
          echo "There is currently no build setup"
      - name: Deploy the site
        env:
          SILLY_TEST_SECRET: ${{ secrets.SILLY_TEST_SECRET }}
        run: |
          touch message.txt
          echo $SILLY_TEST_SECRET
