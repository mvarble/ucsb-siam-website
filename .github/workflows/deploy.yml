name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the site
        run: |
          echo "There is currently no build setup"
  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: deploy
      url: 'https://web.math.ucsb.edu/~siam'
    steps:
      - name: Setup SSH Keys and known_hosts
        run: |
          echo "${{ secrets.SILLY_TEST_SECRET }}"
